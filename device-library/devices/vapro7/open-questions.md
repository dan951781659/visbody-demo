# VAPro7 待补充问题

## 来源

- `/Users/kean/Downloads/VAPro7使用说明(2).pdf`

## 最后更新依据

- 基于 `Visbody-A Pro7` 产品使用说明书建档时识别出的信息缺口

## 确定事实

- `已确认`：当前问题列表用于承接说明书未覆盖、但后续产品设计和功能扩展高频需要的信息
- `已确认`：以下条目均不是已知事实，而是待调研方向

## 设计含义

- `推断`：说明书足以建立设备能力和主流程认知，但不足以支撑细粒度功能设计，因此需要持续补充实机、后台和运营侧材料。
- `推断`：后续每次新需求评估前，应先检查本页是否已有未解问题，避免重复调研。

## 待补充

### 设备端信息架构

- `待验证`：首页有哪些主入口，是否区分不同测量项目
- `待验证`：报告列表页包含哪些字段、排序方式和操作入口
- `待验证`：设置页的导航结构是单层列表还是分组结构

### 视觉与品牌

- `已确认`：设备端默认视觉规范已补充至 `ui-style-spec.md`，包括主题色、卡片样式、图标风格和排版密度
- `待验证`：品牌定制能力是否只支持纸质报告 logo，还是也支持设备端视觉定制
- `待验证`：首页、报告页、二维码页是否展示品牌元素

### 测量过程与异常处理

- `待验证`：姿势错误、用户移动、转台异常、校准失败时如何提示
- `待验证`：测量中断后是否支持重试、续测或结果废弃
- `待验证`：体态、体围、身体成分在设备端是否可独立选择或组合执行

### 报告与数据链路

- `待验证`：设备端报告列表展示的是本机历史、账号历史还是用户历史
- `待验证`：报告从设备生成到管理端、公众号端可见的同步时延
- `待验证`：自动打印的触发逻辑和失败后的补救方式
- `待验证`：用户 ID 加密规则的具体表现形式

### 后台协同

- `待验证`：管理端有哪些与设备强相关的配置项
- `待验证`：后台是否支持多门店、多设备管理
- `待验证`：设备绑定、换机、停用、恢复出厂后的后台流程

### 运维与技术能力

- `待验证`：设备是否有日志导出、故障码、远程诊断或售后模式
- `待验证`：在线更新是否支持灰度、强制更新或版本回退
- `待验证`：是否存在埋点、行为记录或运营统计能力
