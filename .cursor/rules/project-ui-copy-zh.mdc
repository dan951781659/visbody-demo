---
description: APP Demo 项目界面文案默认使用中文
alwaysApply: true
---

# APP Demo 界面文案（中文）

本仓库（`APP Demo` / `motionstation-mobile`）的产品与界面文案**默认使用简体中文**。

## 必须执行

1. **用户可见字符串**（按钮、标题、标签、提示、空状态、导航、Toast、筛选选项等）写入 HTML/TSX/CSS `content` 时使用中文。
2. **与对话语言无关**：用户在对话里用英文描述需求时，仍须将页面展示文案写为中文。
3. **向用户解释说明**（对话回复）可使用中文，与界面中文文案不冲突。
4. **无需每次重复说明**：在本项目中实现或修改 UI 时，直接按中文文案执行，不要向用户再确认「是否用中文」。

## 不适用

- 代码标识符、路由路径、API 字段名、类型名等技术命名仍用英文。
- 用户明确指定某段界面文案使用英文或中英对照时，以当次任务为准。
